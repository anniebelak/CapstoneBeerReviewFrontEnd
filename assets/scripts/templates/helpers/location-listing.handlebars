

{{#each locations as |location|}}
{{#each locations.beers as |beer|}}
<div class="location" data-id="{{location.id}}">
  <h4>Brewery: {{name}}</h4>
    <h4>Address:{{address}}</h4>
    <h4>Beer:{{beer.name}}</h4>
</div>
{{/each}}
{{/each}}
